/* This is a
 * multiline comment
 */
fn main() -> int {
    var y: const int = 5 // This is a normal comment
    var x = 0..y
    for (var i = 0; i < size(x); ++i) {
        x[i] = i
        print(string(i) + " ")
    }

    return 0
}
