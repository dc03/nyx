/* This is a
 * multiline comment
 */
 fn main() -> int {
    var y: const int = 5 // This is a normal comment
    var x: [int, y]
    for (var i = 0; i < x.size; i++) {
        x[i] = i
        print(str(i) + " ")
    }

    return 0
}
